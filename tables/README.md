# Table data

These CSV files contain the processed values used for performance summaries, paired inference, runtime accounting, ablations, noise robustness, depth scaling, confidence-bound analysis, certification diagnostics, quantum shot accounting, fixed-shot reliability, and the experimental protocol.

Run-level evidence is stored under `data/`, and complete statistical outputs are stored under `statistics/`.
